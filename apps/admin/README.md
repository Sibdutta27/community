# React Admin Skeleton

A clean Vite + React admin dashboard skeleton using CSS Modules.

## Run locally

```bash
npm install
npm run dev
```

## Structure

- `src/components` reusable UI components
- `src/layouts` admin layout wrapper
- `src/pages` separate page folders
- `src/routes` app routes
- `src/styles/style.css` global CSS
