# apps/pwa — Community PWA (planned)

Placeholder for the Progressive Web App, a near-term roadmap item (see
[`docs/product/PRD.md`](../../docs/product/PRD.md) §9).

Not yet implemented. When scaffolding it:
- It will consume the same backend API (`apps/api`) as the member web app.
- Follow the member frontend's design system ([`docs/design-system.md`](../../docs/design-system.md)),
  not the admin's.
- Add `build`/`dev`/`lint`/`typecheck` scripts so Turborepo picks it up.
- Add it to `docs/architecture/categories.yaml` via `/community-kb refresh`.
