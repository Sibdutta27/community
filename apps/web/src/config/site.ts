// NOTE: the user-facing metadata title/description are localized via the
// `metadata` namespace in `messages/{en,es}.json` (see `generateMetadata` in
// `src/app/layout.tsx`). These values remain as locale-independent fallbacks.
export const siteConfig = {
  name: "Taíno Nation of Borikén",
  description:
    "Frontend platform for community enrollment, identity tracking, and local service discovery.",
  url: "",
} as const;
