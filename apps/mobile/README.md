# apps/mobile — YucayekeConnect mobile app (planned)

Placeholder for the mobile app (e.g. React Native / Expo), a near-term roadmap item
(see [`docs/product/PRD.md`](../../docs/product/PRD.md) §9).

Not yet implemented. When scaffolding it:

- It will consume the same backend API (`apps/api`).
- Add `build`/`dev`/`lint`/`typecheck` scripts so Turborepo picks it up.
- Add it to `docs/architecture/categories.yaml` via `/community-kb refresh`.
