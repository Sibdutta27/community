{
  "$schema": "https://json.schemastore.org/tsconfig",
  "display": "Community base",
  "//": "Shared base. Apps do NOT extend this yet (kept independent to preserve build parity during the structural migration). Adopt per-app in a follow-up.",
  "compilerOptions": {
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "forceConsistentCasingInFileNames": true,
    "target": "ES2022"
  }
}
